## Netlify Environment Variables (import template)
## Fill in values, then import into Netlify.
##
## SECURITY:
## - SUPABASE_SERVICE_ROLE_KEY is SERVER-ONLY. Do NOT expose it to the browser.
## - Only variables prefixed with NEXT_PUBLIC_ are exposed to client-side code.
##
## Required (client + server)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

## Optional (also set in netlify.toml)
NODE_VERSION=18

